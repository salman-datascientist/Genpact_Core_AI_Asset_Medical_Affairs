# This file keeps the data/ directory tracked in git.
# The actual database files are ignored via .gitignore (data/*)
# The directory and database will be auto-created when the API server first runs.
